// import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

// /**
 // * Usman Khan
 // */
// public class Barrel extends Mover
// {
    // Space currentWorld;
    
    // public Barrel (int direction) {
        // turn(direction);
        // speed = maxSpeed;
    // }
    
    // /**
     // * Act - do whatever the Barrel wants to do. This method is called whenever
     // * the 'Act' or 'Run' button gets pressed in the environment.
     // */
    // public void act() 
    // {
        // currentWorld = (Space) getWorld();
        
        // //Move by speed for every tick of the program
        // move (-5);
       
          // //Wraps mover around the screen
        // if(getX() > 850) {           
                // currentWorld.removeObject(this);
            // }
        // else if(getY() > 650) {
                // currentWorld.removeObject(this);
            // }
        // else if(getX() < -50) {
                // currentWorld.removeObject(this);
            // }
        // else if(getY() < -50) {
                // currentWorld.removeObject(this);
                // }
            // }
// }
